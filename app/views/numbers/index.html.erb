
<!DOCTYPE html>
<html>
<head>
  <title></title>
  <link rel="stylesheet" href="http://www.w3schools.com/lib/w3.css">
</head>
<body>
  <div style="width:70%; margin:auto; text-align: center; ">
    <h1>Welcome to the Great Number Game</h1>
    <h3>I am thinking of a number between 1 and 100</h3>
    <h3>Take a guess!</h3>

    <% if flash[:right] %>
      <div class="w3-green"><%= flash[:right] %></div>
      <a href="/reset" class="w3-btn">RESET!</a>
    <% else %>
      <% if flash[:wrong] %>
        <div class="w3-red">
          <%= flash[:wrong] %>
        </div>
      <% end %>
    
      <form action="/guess" method="post" class="w3-container">
        <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
        <input type="number" name="guess" class="w3-input">
        <input type="submit" class="w3-btn" value="Guess!">
      </form>
    <% end %>
      
    
  </div>
</body>
</html>